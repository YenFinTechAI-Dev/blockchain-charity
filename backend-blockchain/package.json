{
    "name": "backend-blockchain",
    "version": "1.0.0",
    "scripts": {
        "compile": "hardhat compile",
        "node": "hardhat node",
        "deploy": "hardhat run contracts/scripts/deploy.ts --network localhost",
        "deploy:sepolia": "hardhat run contracts/scripts/deploy.ts --network sepolia",
        "test": "hardhat test"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^5.0.0",
        "hardhat": "^2.22.3",
        "ts-node": "^10.9.2",
        "typescript": "^5.8.3"
    },
    "dependencies": {
        "@openzeppelin/contracts": "^5.6.1",
        "dotenv": "^16.5.0",
        "ethers": "^6.14.3"
    }
}
